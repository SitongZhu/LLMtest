# LLMtest 推理/训练依赖（在仓库根目录执行 pip install）
# 注意：请先按 CUDA 版本安装 torch，再装本文件，例如：
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126
#   pip install -r requirements-llmtest.txt
#
# 或直接使用 scripts/setup_env.sh（会按环境变量安装 torch）

huggingface_hub>=0.20.0,<1.0.0
scikit-learn>=1.3.0

-r LLaMA-Factory/LLaMA-Factory/requirements/metrics.txt
-r LLaMA-Factory/LLaMA-Factory/requirements/vllm.txt
-r LLaMA-Factory/LLaMA-Factory/requirements/gptq.txt
